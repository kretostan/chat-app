info:
  name: Load Room
  type: http
  seq: 7

http:
  method: GET
  url: "{{host}}/chat/rooms/:id/messages"
  params:
    - name: id
      value: "2"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

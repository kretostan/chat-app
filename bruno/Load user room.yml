info:
  name: Load user room
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/chat/rooms/:id"
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
